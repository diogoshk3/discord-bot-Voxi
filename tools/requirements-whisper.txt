# tools/requirements-whisper.txt — deps do sidecar de STT (faster-whisper, CTranslate2).
# Instalado por setup-whisper.sh/.ps1 em tools/whisper-venv/ (gitignored). Versões
# PINADAS para resolução determinística (antes instalava `faster-whisper` sem versão
# nenhuma — dois rebuilds podiam puxar releases diferentes, incl. uma comprometida).
# SEM PyTorch nem cmake. Espelha o padrão de requirements-clone.txt/requirements-kokoro.txt.
#
# Versões lidas de uma resolução fresca (sem instalar: `pip index versions` +
# `pip download` para o grafo completo) em 2026-07-14 — NÃO inventadas. Se o VPS já
# tiver uma versão diferente instalada, confirma e ajusta antes do próximo setup.
faster-whisper==1.2.1
ctranslate2==4.8.1
onnxruntime==1.27.0
